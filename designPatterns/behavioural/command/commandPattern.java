package designPatterns.behavioural.command;

/*
Because every action/request go via Invoker,maintaining queue of all the requests
is easy now and if required we can re-invoke in the same or reverse(Undo)in future.
*/

/*
Store the actions and data. This enables use to "Do" or "UNDO". 
Example :  menu structure, Logging events
*/
public class commandPattern {

}
